# This workflow builds Android and iOS SDK artifacts
name: Build Artifacts

on:
  pull_request:
    branches:
      - "main"
      - "develop"
  push:
    branches:
      - "main"
      - "ci/test"

jobs:
  build-artifacts:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0

      - name: Check if version changed
        uses: EndBug/version-check@v1
        id: check

      - name: Use Node.js '16.x'
        uses: actions/setup-node@v2
        with:
          node-version: "16.x"
          cache: "npm"

      - run: npm ci

      # Android artifacts
      - name: Set up our JDK environment
        uses: actions/setup-java@v1.4.3
        with:
          java-version: 1.11

      - name: Install Web3j
        run: curl -L https://gist.githubusercontent.com/CJ42/a2eb3893102eef542c3f0780be5e909c/raw/9072a24654397f52c28776685808c5b8ec15b3e2/web3j-installer-fix.sh | sh && source ~/.web3j/source.sh

      - name: Generate Android artifacts
        run: npx ts-node make-android.ts

      - name: Grant rights
        run: chmod +x java/build.gradle

      - name: Build jar
        working-directory: ./java
        run: gradle build

      # iOS artifacts
      - name: Generate iOS artifacts
        run: |
          mkdir ios
          npx ts-node make-ios.ts

      # Upload Arifacts
      - name: Upload iOS Artifact
        uses: actions/upload-artifact@v2
        if: github.event_name == 'push' && steps.check.outputs.changed == 'true'
        with:
          name: UPContractsAbi.swift
          path: ios/UPContractsAbi.swift

      - name: Upload Android Artifact
        uses: actions/upload-artifact@v2
        if: github.event_name == 'push'  && steps.check.outputs.changed == 'true'
        with:
          name: upcontracts.jar
          path: java/build/libs/upcontracts.jar

      # Trigger Release
      - name: Repository Dispatch
        if: github.event_name == 'push' && steps.check.outputs.changed == 'true'
        uses: peter-evans/repository-dispatch@v1
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          event-type: trigger-release
          client-payload: '{"ref": "${{ github.ref }}", "sha": "${{ github.sha }}"}'
